base: configs/training/full.yaml

model:
  d_model: 128
  n_layers: 4
  n_heads: 4
  d_ff: 512
  max_seq_len: 256

training:
  batch_size: 8
  max_iters: 500
  log_interval: 10
  checkpoint_time_minutes: 999
  early_stop_patience: false
