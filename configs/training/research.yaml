base: configs/training/full.yaml

model:
  d_model: 384
  n_layers: 8
  n_heads: 8
  d_ff: 1582
  max_seq_len: 512
  dropout: 0.1

training:
  optimizer: sophia
  batch_size: 4
  max_iters: 2000
  log_interval: 10
  eval_interval: 100
  checkpoint_time_minutes: 30
  early_stop_patience: 20
