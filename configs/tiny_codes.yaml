data:
  path: data/tiny-codes/tiny-codes.jsonl
  seq_len: 512
  val_split: 0.05
  download_if_missing: true

training:
  batch_size: 8
  max_iters: 20000
  lr: 3.0e-4

model:
  vocab_size: 5000
  d_model: 384
  n_layers: 8
  n_heads: 8
  d_ff: 1536
